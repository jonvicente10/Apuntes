
Para que al abrir una terminal nueva no tengamos que hacer los exports, tenemos que editar el archivo .bashrc

Al final del archivo abria que añadir las siguientes lineas:

export PATH=$PATH:/opt/eclipse/
export PATH=$PATH:/opt/jdk1.8.0_20/bin/
